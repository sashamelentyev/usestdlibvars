// Code generated by usestdlibvars, DO NOT EDIT.

package {{ .PackageName }}

import "net/http"

var (
{{- range $key, $value := .Mapping }}
	_ = "{{ toLower $key }}"
{{- end }}
)

const (
{{- range $key, $value := .Mapping }}
	_ = "{{ toLower $key }}"
{{- end }}
)

var (
{{- range $key, $value := .Mapping }}
	_ = http.Request{Method: {{ $value }}} // want `"{{ quoteMeta (toLower $key) }}" can be replaced by {{ quoteMeta $value }}`
{{- end }}
)

var (
{{- range $key, $value := .Mapping }}
	_ = &http.Request{Method: {{ $value }}} // want `"{{ quoteMeta (toLower $key) }}" can be replaced by {{ quoteMeta $value }}`
{{- end }}
)

var (
{{- range $key, $value := .Mapping }}
	_, _ = http.NewRequest({{ $value }}, "", http.NoBody) // want `"{{ quoteMeta (toLower $key) }}" can be replaced by {{ quoteMeta $value }}`
{{- end }}
)

var (
{{- range $key, $value := .Mapping }}
	_, _ = http.NewRequestWithContext(nil, {{ $value }}, "", http.NoBody) // want `"{{ quoteMeta (toLower $key) }}" can be replaced by {{ quoteMeta $value }}`
{{- end }}
)
